namespace POC.AsyncAwait.levelModerate
{
    internal sealed class Payload
    {
        public string Origin { get; }

        public Payload(string origin)
        {
            Origin = origin;
        }
    }
}
